language: c
install: true
script: make WITH_FAST_SYSCALL=1 test
before_install:
  - sudo apt-get update  -qq
  - sudo apt-get install -qq g++-multilib
  - sudo apt-get install -qq gcc-multilib
#  - sudo apt-get install -qq clang
#  - sudo apt-get install -qq valgrind
